# Kopiere diese Datei zu .env.local und trage deinen eigenen Key ein.
# .env.local wird von Git ignoriert (siehe .gitignore) — Key bleibt privat.

# Kostenloser YouTube Data API v3 Key, siehe README für Anleitung.
YOUTUBE_API_KEY=
